# LibreLane / flow outputs
runs/
*.gds
*.def
*.odb
*.spef

# sv2v generated output — build artifact, never committed (PROJECT_INSTRUCTIONS 4.2)
gen/
*.sv2v.v

# simulator artifacts
obj_dir/
dsim_work/
*.db
dsim.env
cov_report_*/
cov_full/
*.vcd
*.mxd
*.vcd
*.fst
*.wdb
xsim.dir/
*.jou
*.log
*.pb

# python / editor
__pycache__/
.venv/
verif/compliance/work/

# Test-program build artifacts. Regenerate with sw/tests/build.sh <prog>.S
# The .hex IS tracked: run_lockstep.sh consumes it directly, so a fresh clone
# can run lockstep without a RISC-V toolchain installed.
sw/tests/*.o
sw/tests/*.bin
sw/tests/*.elf
